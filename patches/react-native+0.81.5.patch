diff --git a/node_modules/react-native/Libraries/ReactNative/ReactFabricPublicInstance/ReactFabricHostComponent.js b/node_modules/react-native/Libraries/ReactNative/ReactFabricPublicInstance/ReactFabricHostComponent.js
index aef54de..fb75b72 100644
--- a/node_modules/react-native/Libraries/ReactNative/ReactFabricPublicInstance/ReactFabricHostComponent.js
+++ b/node_modules/react-native/Libraries/ReactNative/ReactFabricPublicInstance/ReactFabricHostComponent.js
@@ -27,13 +27,17 @@ import {create} from './ReactNativeAttributePayload';
 import warnForStyleProps from './warnForStyleProps';
 import nullthrows from 'nullthrows';
 
-const {
-  measure: fabricMeasure,
-  measureInWindow: fabricMeasureInWindow,
-  measureLayout: fabricMeasureLayout,
-  getBoundingClientRect: fabricGetBoundingClientRect,
-  setNativeProps,
-} = nullthrows(getFabricUIManager());
+// Lazily initialized to avoid accessing nativeFabricUIManager before the
+// Fabric binding is installed on the JS runtime (it's set asynchronously
+// via the bridgeless bindings executor, so it may be null at module-load
+// time in some Expo / source-build configurations).
+let _fabricManager: ReturnType<typeof getFabricUIManager> | null = null;
+function getFabricManager() {
+  if (_fabricManager == null) {
+    _fabricManager = nullthrows(getFabricUIManager());
+  }
+  return _fabricManager;
+}
 
 const noop = () => {};
 
@@ -70,7 +74,7 @@ export default class ReactFabricHostComponent implements NativeMethods {
       this.__internalInstanceHandle,
     );
     if (node != null) {
-      fabricMeasure(node, callback);
+      getFabricManager().measure(node, callback);
     }
   }
 
@@ -79,7 +83,7 @@ export default class ReactFabricHostComponent implements NativeMethods {
       this.__internalInstanceHandle,
     );
     if (node != null) {
-      fabricMeasureInWindow(node, callback);
+      getFabricManager().measureInWindow(node, callback);
     }
   }
 
@@ -109,7 +113,7 @@ export default class ReactFabricHostComponent implements NativeMethods {
     );
 
     if (toStateNode != null && fromStateNode != null) {
-      fabricMeasureLayout(
+      getFabricManager().measureLayout(
         toStateNode,
         fromStateNode,
         onFail != null ? onFail : noop,
@@ -123,7 +127,7 @@ export default class ReactFabricHostComponent implements NativeMethods {
       this.__internalInstanceHandle,
     );
     if (node != null) {
-      const rect = fabricGetBoundingClientRect(node, true);
+      const rect = getFabricManager().getBoundingClientRect(node, true);
 
       if (rect) {
         return new DOMRect(rect[0], rect[1], rect[2], rect[3]);
@@ -144,7 +148,7 @@ export default class ReactFabricHostComponent implements NativeMethods {
       this.__internalInstanceHandle,
     );
     if (node != null && updatePayload != null) {
-      setNativeProps(node, updatePayload);
+      getFabricManager().setNativeProps(node, updatePayload);
     }
   }
 }
